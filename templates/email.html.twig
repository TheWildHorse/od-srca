
{% extends 'base.html.twig' %}

{% block title %}Od Srca{% endblock %}

{% block body %}
    <style>
        body{
            background-image: url("{{ app.request.getSchemeAndHttpHost() }}/images/background.png");
        }
        .paragraph,
        .title{
            line-height: 30px;
            color: #4d4d4d
        }
        .email-header > img {
             width: 100px;
             align-self: center;
             justify-self: right;
             border-radius: 0;
         }
        .email-header{
            display: grid;
            grid-template-columns: 40% 60%;
            max-width: 500px;
            grid-gap: 5%;
        }
        .email-page{
            max-width: 500px;
            font-family: 'Quicksand', sans-serif;
            overflow-x: hidden;
            background: white;
            margin: 5% auto;
            padding: 20px;
            border-radius: 24px;
            box-shadow: 0px 2px 6px grey;
        }
        .title{
            font-size: 3rem;
            margin: 5% 0 0 0;
            align-self: center;
            line-height: 40px;
        }
        .paragraph.details{
            display: grid;
            grid-template-columns: 50% 50%;
            width: 80%;
            margin: auto;
        }
        .paragraph.details > p,
        .paragraph.details > div{
            display: grid;
            grid-template-rows: 40px 40px 70px 50px 40px auto auto;
        }
        #wishImage{
            width: 100%;
            display: grid;
            border-radius: 5px;
        }
    </style>
    <div class="email-page">
        <div class="email-header">
            <img src="{{ app.request.getSchemeAndHttpHost() }}/images/logo.svg"/>
            <h1 class="title">Od Srca</h1>
        </div>

        <div class="email-content">
            <p class="paragraph">
                Hvala vam što ste se odlučili ispuniti želju, u nastavku slijede informacije o primatelju:
            </p>
            <div class="paragraph details">
                <p>
                   <b>Ime:</b>
                   <b>Prezime:</b>
                    <b>Adresa:</b>
                    <b>e-mail:</b>
                    <b>Broj mobitela:</b>
                    <b>Želja:</b>
                    <b>Slika želje:</b>
                </p>
                <div>
                    <p>{{ name }}</p>
                    <p>{{ lastname }}</p>
                    <p>{{ address }}</p>
                    <p>{{ email }}</p>
                    <p>{{ phone }}</p>
                    <p>{{ wish }}</p>
                    <a href="{{ app.request.getSchemeAndHttpHost() }}{{ '/uploads/images/' ~ wishImage }}">
                        <img id="wishImage" src="{{ app.request.getSchemeAndHttpHost() }}{{ '/uploads/images/' ~ wishImage }}"/>
                    </a>
                </div>
            </div>
            <p class="paragraph">
                Ukoliko ne želite ispuniti ovu želju, molimo Vas da kliknete <a href="{{ currentHost }}/wish/undo/{{ wishId }}">ovdje</a>.
            </p>
        </div>
        {#<div class="hearts">#}
            {#<img src="{{ '/images/illustration.svg' }}"/>#}
        {#</div>#}
    </div>



{% endblock %}